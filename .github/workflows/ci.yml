name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust nightly (pinned)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-03-30
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      - name: Build contracts (wasm32)
        run: cargo build --release --target wasm32-unknown-unknown --workspace --lib

      - name: Test (host VM)
        run: cargo test
